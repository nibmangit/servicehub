export default function ApplyProviderPage() {
  return (
    <div className="max-w-6xl mx-auto px-6 py-10">
      <h1 className="text-2xl font-bold text-(--color-foreground)">Become a Provider</h1>
      <p className="text-(--color-muted-foreground) mt-1">Built in step 10 (identity verification first, then this).</p>
    </div>
  );
}